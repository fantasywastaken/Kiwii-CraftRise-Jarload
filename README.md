# Kiwii-CraftRise-Jarload
A hybrid C++ DLL + Java agent utility client for CraftRise (Rise Client 1.8.9), built around runtime reflection so it survives every obfuscation pass Rise ships without rebuilding.
